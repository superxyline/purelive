org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -Dconsole.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
# systemProp.http.proxyHost=127.0.0.1
# systemProp.http.proxyPort=7897
# systemProp.https.proxyHost=127.0.0.1
# systemProp.https.proxyPort=7897
# systemProp.socks.proxyHost=127.0.0.1
# systemProp.socks.proxyPort=7898
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
org.gradle.dependency.verification=off
org.gradle.warning.mode=none

org.gradle.offline=true
# 注：曾尝试 org.gradle.parallel/caching/nonTransitiveRClass 提速，
# 但与本项目根脚本的 subprojects/afterEvaluate/evaluationDependsOn 手写逻辑死锁（组合构建等待），已回退。
# 提速靠：关闭 R8 混淆 + 关闭 release lint（见 app/build.gradle.kts）+ 复用 Gradle daemon。
