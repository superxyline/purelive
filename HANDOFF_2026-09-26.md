# 交接文档 — 2026-09-26（四修复 + 竖屏直播 + 礼物开关默认值 + v2.2.1 首次发行版）

> 仓库基线：`custom` 分支 HEAD = `f6329e18`（version 2.2.1+4019），Gitee/GitHub 均已同步，tag `v2.2.1` 已推两端。
> 设备状态：**平板（990c20c5）与手机（41e218f1，192.168.31.189）均已装**，但装的是 `81cbd1be` 构建（App 内版本号仍显示 2.2.0+4018）；与发行版 2.2.1 构建的差异**仅 pubspec 版本号**，功能代码完全一致，无需为版本号重装。
> 用户测试状态：用户表示已自测完毕，**"需要改的话会给反馈"**——本轮遗留项只等用户点单，不要主动去改。

## 一、本轮四个 bug 修复（用户报障，全部完成）

提交 `108be5a9`，细节与根因：

1. **斗鱼「读取视频信息失败」**：`douyu_site.getPlayQualites` 走老接口 getH5Play 且用 `detail.data` 拼表单（getRoomDetail 从未赋值 data，表单里是字面量 `"null"`）→ 接口报错无校验直接空指针。修复=与取流同链路 `DouyuUtils.sign(rate:-1)` + `getH5PlayV1` + 错误码/结构校验。**平板实测通过**（原画1080P30 正常播）。签名链验证脚本在 `E:\codex\_douyu_test\`。
2. **备份「提示授予权限」死循环**：Manifest 未声明 MANAGE_EXTERNAL_STORAGE + targetSdk 37 → `requestStoragePermission` 必拒。修复=移动端改 `FilePicker.saveFile`（SAF 另存为，零权限），桌面保留原目录选择。**用户手动验证通过**。
3. **登录页换号被踢**：斗鱼/快手/抖音/虎牙四个 `*_web_login_controller` 删除自动检测（onLoadStop 判定 / 2s 轮询），只保留右上角「完成」(saveAndClose) 抓 cookie；douyu page 删了 onLoadStop 引用。B站扫码不受影响。
4. **快手关注同步**：`FollowSyncService.syncKuaishou` + 账号页快手卡片 `onSync` 入口。接口从快手 web bundle（`pc-live-next/js/follow.js`）抠出：`GET /live_api/follow/living`（直播中）+ `GET /live_api/follow/all?pcursor=`（全部，`pcursor=="no_more"` 终止），匿名时静默返回空 list。**响应 item 字段是防御性解析（`_kuaishouRoomFromCard`），未经真实登录态验证**——若用户反馈列表空/报错，抓 `live_api/follow/all` 真实响应对字段即可。

## 二、竖屏直播（用户要求）

- **方向逻辑** `orientation_policy.dart`：默认分支改为竖屏源（`isVerticalVideo`，解码宽高 h≥w）**+ 手机 → 竖屏全屏**；平板保持横屏全屏。手机判定 `isPhoneSize` 用**物理对角线 <6.5 英寸**（短边 dp 会把小米平板5 的 582dp 误判成手机）。已知边界：出画前点全屏仍按旧逻辑横屏。
- **竖屏全屏控制条修复** `video_controller_panel.dart`（`15f31aa6`）：顶/底栏 SafeArea 在竖屏方向时不吃状态栏与手势条 insets（横屏保持挖孔避让）。修的是「图标被挤没 + 隐藏后顶栏露半截」。
- **弹幕铺满黑区是用户预期行为**——曾做过"竖屏弹幕收窄到内容条"，**用户明确否决已还原**，以后不要再改弹幕区对齐（见记忆 [[pure-live-android-fixes-20260926]]）。
- 上游未接项仍开放：竖屏三档自动布局、下滑隐藏弹幕栏手势。

## 三、礼物开关默认关闭（`81cbd1be`）

`danmaku_settings_controller.dart` 三个开关首次安装默认 `false`（含 toJson/fromJson/extractConfig 兜底同步改）：
- `showLocalGiftFullscreenEffect`（本地礼物全屏特效）
- `showFullscreenGiftCard`（全屏礼物卡片）
- `defaultShowDanmakuListGiftCard`（弹幕列表礼物卡片，按房间未设置时的兜底默认）
用户手动开启后不受影响（hive 键已存在则沿用已有值）。

## 四、遗留：抖音礼物卡片不显示（未定位，日志已埋）

用户报「抖音的礼物没有卡片显示」。静态排查已排除：proto 字段齐全、`WebcastGiftMessage` 分发正常、GiftCard 渲染平台无关、`fullscreenUI` 竖屏也置位。**已埋正式日志**（release 下也写文件）：
- `douyin_danmaku.dart` `[douyin-gift] decoded ...`（解码层）
- `live_play_controller.dart` `[gift-card] enter/queued ...`（开关/全屏/屏蔽判定层）

**诊断流程**：设置 → 备份与恢复 → 开启本地日志（enableLog）→ 进抖音房等礼物 → 「在浏览器查看日志」或拉 `getDownloadsDirectory()/logs/log` → grep `[gift-card]`：
- 两处都没有 → 抖音侧没收到/没解出 gift 消息（查 WS 订阅 method）
- 有 enter 无 queued → 看 enter 日志里 switch/fullscreen 哪个是 false
- 有 queued 无卡片 → UI 层（查全屏态与 fsGifts）

## 五、发行版 v2.2.1（两端状态）

- **GitHub Release 完成**：https://github.com/superxyline/purelive/releases/tag/v2.2.1 —— APK 附件已上传（`pure_live_v2.2.1_arm64.apk`，110.2MB）
- **Gitee Release 创建完成但无附件**：`100 MB` 硬限制，APK 110.2MB 传不上去（已实测 400「文件大小已超出限制：100 MB」）。Release body 已 PATCH 更新为指向 GitHub 直链下载。
  - 体积构成：libffmpegkit.so 30MB + libmpv 21MB + libapp 16MB + libflutter 11MB…纯 arm64 无冗余，二进制重压无解。
  - 瘦身候选（**需用户点单**）：ffmpeg_kit 若确认 0 引用可删（省 30MB，即达 Gitee 限制内）；此前上游分析已标注「录制功能与删依赖二选一」。
  - Gitee API 备忘：创建 release body 需 `access_token` 传 JSON；更新用 **PATCH 且必须带 tag_name+name**（PUT 405）；附件 `attach_files` token 必须放 **query 参数**（放 header/body 均 401）。
- 发行说明为 `C:\Users\13017\AppData\Local\Temp\release_notes.md`（已用尽可删）；发布脚本 `publish_release.py` 同目录。

## 六、June6699/dart_simple_live fork 分析（未点单）

2026-09-25 完成分析（**只分析未动代码**），完整三级清单在记忆 [[pure-live-june-fork-candidates]]，素材 `E:\codex\_dsl_june\june\`。A 级五项（B站分类换老接口/429 防护/抖音弹幕重写/Cookie 分层/防误判下播）最值得先做——其中 B站分类接口 `second/getList+w_webid` 在定制版 `bilibili_site.dart:229` 注释里自认 intermittent 失效中。用户尚未点单。

## 七、环境备忘（本轮新坑）

- **手机无线调试**：192.168.31.189，端口会变（43409→36669 都用过），连接被拒就 `adb mdns services` 重新发现；锁屏会让 adbd 掉线。
- **触控坐标换算**：截屏 PNG 可能是物理横屏（如 2560×1600）而 `input tap` 是逻辑坐标——先 `wm size` 确认，按**同轴比例**换算（之前记忆里的 ×1.28 反推曾两次点错页）。
- `uiautomator dump` 对 Flutter 页面只能拿 `content-desc`（无 text 属性）；dump 前先截图确认当前页面（用户可能同时在操作设备）。
- `/tmp` 是 Git Bash 映射的 Windows 临时目录，跨工具引用没问题；python heredoc（`python - <<'EOF'`）在本环境可用。
- 构建门禁照旧：`flutter analyze lib` 0 error → `flutter build apk --release --target-platform android-arm64` → 产物 `build/app/outputs/flutter-apk/app-release.apk`。

## 八、未提交/来历不明的工作区残留

仓库根有未跟踪文件 `index.html`、`js/`、`static/`、`test/widget_test.dart`——**非本会话产物**，来历不明，未动未提交；处理前先向用户确认。
