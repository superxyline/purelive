# 纯粹直播（纯净版）

<p align="center">
  <img src="assets/icons/icon.png" width="150" alt="纯粹直播（纯净版）图标"/>
</p>

<h4 align="center">基于 Flutter 的安卓多平台直播聚合播放器（自用修改版）</h4>

本项目是开源项目 [pure_live](https://github.com/liuchuancong/pure_live) 的**个人自用修改版**，在原版基础上做了大量裁剪与功能增强，专注 Android 平台，仅保留哔哩哔哩 / 斗鱼 / 虎牙 / 抖音 / 快手五大平台。

- **当前版本**：`2.2.0+4018`（基于上游 v2.1.4 裁剪定制，最后更新：2026-09-23）
- **应用包名**：`com.superxyline.purelive`
- **目标平台**：Android（arm64）
- **代码仓库**：[https://gitee.com/superxyline/purelive](https://gitee.com/superxyline/purelive)

![Pure Live 界面预览](assets/images/banner.png)

> 本仓库**不提供预编译安装包**，如需使用请自行构建，详见下方「自行构建」章节。

---

## 🖥️ NAS 网页端（新增）

除安卓客户端外，本项目内置一套可部署在 NAS 上的**纯 Web 版**（Vue3 + Naive UI 前端 + Node 聚合后端），支持 B站 / 斗鱼 / 虎牙 / 抖音 / 快手五平台观看、弹幕、分区浏览、全平台搜索、B站扫码登录与发弹幕。旧版 Flutter Web 已移除，默认界面即 `nas/webui`，界面为 **Twitch 结构 + 天蓝主色**主题：

- 平台 API 聚合与取流签名（B站 WBI、斗鱼 ub98484234、抖音 a-bogus、虎牙 anticode）全部在服务端执行，浏览器无 CORS/风控问题；
- 弹幕 WebSocket 代理（B站 brotli、斗鱼 STT、虎牙 Tars、抖音 protobuf、快手轮询 → 统一 JSON）；
- 直播间弹幕体验：右侧弹幕/礼物双页签；**上滑回看不被新弹幕打断**（未读计数 + 一键回底 + 10 秒自动恢复）；**弹幕屏蔽**（普通词 / 正则 `/xxx/` / 通配 `*?` + 屏蔽用户，右键弹幕快捷屏蔽）；播放器内**飘屏弹幕**与**左下角礼物卡片**（安卓同款：价格分档显示时长 / 3 秒合并 / 堆叠），右上角独立开关；
- 分区浏览：导航「分区」或首页「按分区浏览」进入，选平台 → 分类 → 子分区看房间网格（子分区按 `areaId` 请求，与各平台接口对齐）；
- 全平台搜索：房间搜索默认五平台并发检索并交错合并，另有**主播搜索**（B站/斗鱼/虎牙），结果卡片带平台角标，支持加载更多；
- 播放增强：**7 秒无画面自动换线**、B站 **CDN 线路 TCP 测速排序**、**HEVC 开关**（浏览器硬解可用时显示）、清晰度/音量/静音记忆（音量按房间记忆）；
- 关注列表**服务端持久化**（关注变化自动写穿 NAS，换浏览器/清缓存自动恢复）+ **每分钟轻量开播轮询**；关注与安卓 App 经 NAS 双向同步；
- cookie 托管（AES-256-GCM 加密落盘，支持退出登录）+ 流媒体 Referer 回退代理；支持手机 App 扫码同步关注列表与登录态；
- Web 播放器：HLS 走 hls.js、flv 走 mpegts.js，直连失败自动回退后端代理。

### 部署到 NAS（详细步骤）

**前置条件**

- 一台已安装 Docker（含 `docker compose` v2 插件）的 NAS / Linux 设备（实测飞牛 fnOS）；
- 一台能跑 **Node 18+** 的机器用于构建前端（本地 Windows / Linux / macOS 均可）；
- 无公网镜像源的 NAS 需本地已有 `nginx:1.27-alpine` 与 `node:22-alpine` 镜像（server 的 Dockerfile `FROM` 改成本地已有镜像即可，不要用 `node:20`）。

**1. 规划 NAS 上的部署目录**

以仓库内 [`nas/deploy/docker-compose.yml`](nas/deploy/docker-compose.yml) 为模板（其中 `../server`、`../webui-dist`、`../data` 均为相对 compose 文件的路径）：

```
purelive-nas/
├── server/                 # 后端源码：仓库 nas/server/ 整目录上传（去掉 node_modules）
├── webui-dist/             # 前端产物：本地构建的 dist/ 内容（只覆盖内容，勿删目录本身）
├── data/                   # 运行数据（加密 cookie、关注列表）——务必纳入备份
├── certs/                  # 自签证书（走 8091 HTTPS 时需要，可选）
└── deploy/
    ├── docker-compose.yml  # 直接取自仓库 nas/deploy/
    ├── Dockerfile.server
    └── nginx.conf
```

**2. 构建前端**

```bash
cd nas/webui
npm install       # 首次需要
npm run build     # 产出 dist/
```

把 `dist/` 目录的**内容**上传覆盖到 NAS 的 `webui-dist/`（nginx 以 bind mount 挂载该目录，**不要删除目录本身**）。前端纯静态资源，替换后无需重启 nginx 容器也常能直接生效，但建议执行第 4 步的 recreate。

**3. 启动服务**

```bash
cd purelive-nas/deploy
sudo docker compose up -d --build     # 用户已在 docker 组时可去掉 sudo
```

首次启动会构建 `server` 镜像并创建两个容器：`purelive-server`（Node/Fastify 聚合后端）与 `purelive-web`（nginx:1.27-alpine）。

**4. 访问与验证**

浏览器打开 `http://<NAS-IP>:8090`（推荐）或 `https://<NAS-IP>:8091`（自签证书，首次需手动信任）。

**5. 后续更新前端**

```bash
cd nas/webui && npm run build
# 覆盖 dist/ 内容到 NAS 的 webui-dist/ 后：
sudo docker compose up -d --force-recreate web
```

> ⚠️ 两个必踩的坑：
> - **bind mount 换内容后必须 `--force-recreate web`**：容器内仍是旧 inode，`nginx -s reload` 不生效，表现为 403 或一直旧页面；单文件挂载的 `nginx.conf` 改动同理。
> - **`index.html` 必须 no-cache**：强缓存会导致新版本不生效，[`nas/deploy/nginx.conf`](nas/deploy/nginx.conf) 已配置 `Cache-Control: no-cache`，勿删。

架构图、环境变量（`NAS_MASTER_KEY` 等）、登录与发弹幕、安全注意事项见 [nas/README.md](nas/README.md)。安卓客户端构建不受影响。

---

## 🔄 原版与自用修改版的区别

| 项目 | 原版 pure_live | 自用修改版（纯净版） |
| --- | --- | --- |
| 定位 | 通用开源项目，多平台 | 个人自用，专注安卓 |
| 目标平台 | Android / Windows / Linux / macOS / iOS | 仅 Android（arm64） |
| 直播平台 | B站、斗鱼、虎牙、抖音、快手、网易CC、Twitch、SOOP、IPTV 等 | 仅 B站、斗鱼、虎牙、抖音、快手 |
| 直播录制 | 支持 | 已移除 |
| 历史记录 | 支持 | 已移除 |
| 桌面端 | 支持 | 已移除 |
| 关注页签 | 单一关注列表 | 已开播 / 未开播 / 已关注 三态页签 |
| 开播时长角标 | 无 | 关注列表实时显示开播时长，可开关 |
| 赛事中心 | 无 | CS / LOL / VALORANT 赛程赛果与关注 |
| 醒目留言（SuperChat） | 无 | B站 SC 卡片 + 全屏醒目留言弹窗 |
| 真实发送弹幕 | 仅本地回显 | 登录 B站后可真实发送弹幕 |
| 双开观看 | 无 | 副窗口同时观看两场直播，小窗可拖动/互换 |
| PiP 小窗弹幕 | 无 | 画中画 / 悬浮窗继续看弹幕 |
| 本地互动 | 无 | 本地用户资料、礼物、等级与经验体系 |
| 本地数据加密 | 明文存储 | AES + 系统密钥加密存储 |
| 多平台网页登录 | 部分 | B站 / 斗鱼 / 虎牙 / 抖音 / 快手 网页登录 |
| 跨端数据同步 | 无 | WebDAV 云端备份 + 局域网扫码一键同步 + NAS 服务器常连同步（关注并集双向同步，登录态托管到 NAS） |
| 横竖屏 | 跟随系统 | 平板恒横屏，手机全屏 / 竖屏智能切换 |
| 观看数据口径 | 固定 | 热度 / 真实在线人数按平台自由切换 |
| 主题与字体 | 基础 | 动态取色、加载动画、字体库、字号精细调节 |
| 已知修复 | — | 全屏下滑动返回误退直播间、直播间返回失效、返回后横屏状态泄漏、小米手势条兼容、小白条沉浸（关闭系统导航栏强制对比遮罩，首页列表可延伸到手势条下方滑动，参照 PiliPlus 的 edge-to-edge 实现）、弹幕拖动后自动滚底恢复、斗鱼纯音频、非全屏左右滑动调节亮度/音量失效、抖音新版分享短链（v.douyin.com 不再 302 直接返回渲染页）解析失败等 |

---

## ✨ 特色功能

| 功能 | 说明 |
| --- | --- |
| 五平台聚合 | 专注哔哩哔哩 / 斗鱼 / 虎牙 / 抖音 / 快手五大直播平台，页签选中色跟随平台品牌色 |
| 关注三态页签 | 已开播 / 未开播 / 已关注三页签，已关注页纯净卡片展示 |
| 开播时长角标 | 关注列表实时显示主播已开播时长，可一键开关 |
| 赛事中心 | CS / LOL / VALORANT 赛程与赛果，支持关注比赛与筛选（不再提供从赛事点击跳转直播间） |
| 醒目留言 | B站 SuperChat 弹幕卡片展示与全屏醒目留言弹窗；仅 B站 会产出该类消息，其设置项只在 B站 直播间显示 |
| 真实发送弹幕 | 登录 B站后可真实发送弹幕，不再只是本地回显 |
| 双开观看 | 副窗口同时观看两场直播，小窗可拖动缩放、点击互换主副 |
| PiP 小窗弹幕 | 悬浮小窗播放时继续看弹幕，样式可自定义 |
| 本地互动 | 本地用户资料、平台礼物、等级与经验经济体系 |
| 本地数据加密 | 设置与关注数据 AES + 系统密钥加密存储 |
| 横竖屏适配 | 平板横屏观看，手机全屏 / 竖屏智能切换 |
| 观看数据口径 | 热度 / 真实在线人数按平台自由切换 |
| 备份与传输 | WebDAV 云端备份恢复，局域网扫码一键同步 |
| NAS 常连同步 | 设置 → 备份与恢复 → 「NAS 服务器同步」：填一次 NAS 地址长期有效，关注列表与 Web 端并集双向同步（拉取只加不删、变更 3 秒防抖自动推送），登录态可推送到 NAS 托管，换设备可恢复 |
| 多语言 | 简体中文 / English 自由切换 |
| 主题定制 | 明暗模式、主题色、动态取色、加载动画 |
| 字体与文字 | 字体库下载管理、字号精细调节、全局文字缩放 |
| 弹幕深度定制 | 弹幕模板、位置、样式、速度、描边、透明度与手势互动 |
| 播放器内核 | 多内核切换、首选清晰度、后台播放与悬浮窗 |
| 关注管理 | 标签分组、自动刷新、缩略图刷新与紧凑模式 |
| 导航自定义 | 底部页签排序与显隐自由配置 |
| 网络代理 | 应用与播放器独立代理设置 |
| 工具箱 | 剪贴板链接识别、直播源解析 |
| 多端网页登录 | B站 / 斗鱼 / 虎牙 / 抖音 / 快手网页登录（快手支持网页登录自动取 cookie 或手动粘贴） |
| 桌面快捷方式 | 观看时长最长的 3 位主播 + 赛事中心快捷方式，冷启动直达 |
| 弹幕互动增强 | 点击/长按弹幕弹出操作菜单，支持复制、加一、屏蔽用户/关键词 |
| 弹幕分词屏蔽 | 智能分词选择要屏蔽的关键词，支持手动输入自定义关键词 |
| 观看统计 | 按直播间维度本地累计观看时长与观看期间收到的礼物；礼物价值按各平台公开充值口径折算，并给出主播到手估算（比例非官方数据，界面标注为估算）。斗鱼、快手礼物无价格信息，只统计件数 |
| 直播间信息 OSD | 全屏模式下显示平台/房间号/主播/清晰度/分辨率等信息 |
| 礼物卡片展示 | 弹幕列表中以卡片形式展示礼物（支持B站/斗鱼/虎牙/抖音）；全屏左下角实时弹出堆叠卡片：礼物到达立即弹出、3秒内同用户同礼物合并数量、按价格分档显示时长，本地礼物（自己发送）全屏/非全屏均可见，液体玻璃质感，平台色竖条 + 礼物图标放大展示，同礼物合并数量时数字跳动动画 |
| 全屏礼物卡片开关 | 全屏控制栏礼物图标一键开关左下角礼物卡片，关闭立即清空已显示卡片，与设置页开关同步 |
| 赛事热度排序 | 赛事页面默认显示当天热度最高赛事，无需手动刷新 |
| 赛事秒开 | 赛事数据本地持久缓存：启动先展示缓存内容，后台静默刷新 |
| 弹幕屏蔽增强 | 支持正则表达式、通配符匹配与智能分词选择屏蔽 |
| 礼物效果设置 | 独立控制本地礼物全屏动效与全屏左下角礼物卡片显示；点击礼物卡片可屏蔽该礼物在本直播间的卡片展示，屏蔽列表中可恢复；弹幕列表礼物卡片支持按直播间单独开关（默认开，缺省视为显示） |
| 直播间粉丝数 | 直播间信息中展示主播粉丝数（B站 / 虎牙 / 斗鱼 / 抖音 / 快手）；接口拿不到的平台直接不显示该行，不填假数据 |
| 播放容错 | 双保险：① B站取流时把**实测拿不到视频轨**的 HEVC-flv 线路排到 HLS/ts/fmp4 之后（这类线路只有声音没有画面），因此默认就会选到能出画的线路；② 若开播 7 秒后仍在播却没有有效画面，自动切换到下一条线路（等价于手动切线路），恢复成功不再弹出解码失败提示 |
| 画面遮挡块 | 直播间右上角菜单里的「遮挡块 1/2/3」（每个直播间最多 3 个）：打开后在画面上放一个模糊框遮住该位置的广告，拖动顶部手柄移动、右下角手柄缩放。位置与大小按直播间**分槽位**记住，关掉只是隐藏、**位置保留**，下次打开原样恢复。模糊框位于弹幕层下方（不遮弹幕、不挡点击），手柄只在控制条显示时出现 |
| 全屏浮层透明度 | 互动体验中可分别调节全屏醒目留言（SC）与全屏礼物卡片的不透明度（10%~100%，默认 100% 即原有观感） |
| 屏蔽数据同步 | 本直播间礼物屏蔽随 WebDAV 备份 / 本地备份 / 跨端传输一并同步 |
| 一键临时静音 | 直播画面右上角一键静音，只静音播放器不动系统媒体音量；再点按钮、滑动调音量、音量键、退出重进均自动恢复声音 |
| 更新提醒 | 启动时自动检测 gitee 上是否发布了新版本（可在设置中关闭），弹窗提醒并一键跳转项目主页 |
| 刘海屏适配 | 全屏画面延伸进刘海/挖孔区铺满整屏，控制栏、礼物卡片等控件自动避让 |
| 冷启动刷新优化 | 关注列表批量刷新改用轻量取数：只取卡片需要的字段，跳过弹幕服务器发现、弹幕签名、粉丝数抓取等只有进房间才需要的请求（B站每房间请求数约 4 → 2、斗鱼 3 → 1、虎牙 2 → 1）；刷新批次并发默认提高到 6，并且每批就绪即增量刷新界面（不再等全部房间刷完），手机端也会显示刷新进度条。同时修掉一处数据损坏：详情请求失败时平台返回的空房间对象不再覆盖并落盘，避免一次瞬时超时把卡片清空 |
| 直播间数据优化 | 抖音真实在线人数读取优化（Feed 顶层字段优先）；B站房间详情接口 WBI 风控自动重试 |
| Anime4K 超分 | mpv 内核加载 Anime4K shader，低清直播源画质增强（效率/质量两档可选，仅 media_kit 内核） |
| 直播缓冲调节 | 直播流预读缓冲大小可调，抗网络抖动与低延迟自由取舍 |
| 音量均衡 | loudnorm 音频滤镜拉平不同直播间与直播过程中的音量波动 |
| 编码偏好与CDN测速 | B站 HEVC(H.265) 优先省流量；取流前对 CDN 节点测速，线路按延迟自动排序 |
| 设置搜索 | 设置页支持按名称搜索，直达对应设置分组 |
| 双指缩放画面 | 全屏播放双指捏合缩放（1~4倍）、双指/单指拖动画面位置，右上角一键还原，退出全屏自动复位 |
| 弹幕操作菜单 | 点击/长按弹幕弹出操作菜单；全屏/平板为右侧滑出面板，不遮挡画面 |
| 弹幕回看体验 | 上滑回看历史弹幕，10秒无操作自动回到底部继续滚动；滑回底部立即恢复；支持新消息计数与一键回底 |
| 长按快速全屏 | 长按直播间卡片弹出菜单支持"全屏播放"，一键进入直播间并从首帧起即为横屏全屏，无形态切换 |
| 分区自动标签 | 关注页标签栏自动按直播间分区生成标签，跨平台同分区归为一类（如斗鱼/虎牙的CS2），自动跟随分区变化 |
| 页签双击刷新 | 首页「关注」「热门」页签双击即刷新当前列表，手机底部导航与平板侧边栏通用 |
| 礼物协议修复 | 虎牙礼物改用 uri 6501（SendItemSubBroadcastPacket）真实结构解析；B站礼物昵称/图标/实付金额字段修正；抖音礼物改用钻石单价并支持图标回退 |
| 礼物目录增强 | 虎牙弹幕连接后经 WUP getPropsList 拉取礼物目录，礼物卡片显示准确名称/图标/按单价分档时长（旧消息缺字段时自动补齐）；B站新 protobuf 协议 SEND_GIFT_V2 已支持（灰度房间礼物不再丢失） |
| 虎牙未登录回退 | 未登录虎牙时接口被风控返回"未开播"的场景下，自动以隐藏 WebView 加载房间页取回真实开播状态与播放流 |
| 快手平台支持 | 快手直播观看（房间页解析+匿名会话引导）、分类分区与推荐、弹幕评论与真实在线人数（移动端增量 Feed 轮询）、账号页网页登录自动取 cookie 或手动粘贴；本地互动含快手专属礼包（快币/等级/礼物），发送数据结构与其它平台一致；链接解析支持 live.kuaishou.com/u/{id} 与 /s/{短码}。已知限制：快手官方未开放房间搜索 API；真实平台礼物消息与发送弹幕需登录态签名协议，暂不支持 |
| 平台色视觉点缀 | 热门/关注页平台页签选中色跟随平台品牌色；关注卡片开播时长角标内嵌平台名（如"虎牙 · 2小时19分"）；账号页已登录平台以品牌色淡底突出、未登录弱化显示 |
| 抖音搜索三级回退 | 移植上游 DouyinSearch：匿名 live 搜索 → 通用流式搜索 → 公开分区检索（双域名备用），被风控静默降级不再报"搜索被限制"（NAS server 端同步移植） |
| 纯 Dart 签名 | 抖音 a-bogus 换上游纯 Dart `ABogus`；斗鱼换 `DouyuUtils`（getEncryption 描述符 + getH5PlayV1 纯 Dart 签名，过期自动强刷重试），不再依赖 flutter_js 运行时；斗鱼 `parsePlayUrl` 防完整地址双重拼接 |
| 重复/相似弹幕过滤 | 短窗口相同文案合并（窗口 1–30 秒可调）+ 编辑距离相似度过滤（阈值 50–100），默认关；设置页新增两组开关，Web 端弹幕屏蔽面板同步支持 |
| 关注复合身份 | 关注判重、状态更新与标签映射统一 `平台:房间号` 复合身份（上游 2.5.1），不同平台相同房间号不再互相覆盖，旧标签数据读侧兼容 |
| 竖屏沉浸方向 | 全屏方向策略（默认强制横屏保持旧行为，可选跟随系统 / 跟随直播源）+ 控制条「方向」按钮按房间循环 自动/强制竖/强制横 + 竖屏全屏 contain 画面的封面模糊沉浸背景（ambient 蒙层 15%） |
| 弹幕回看误暂停修复 | 程序滚动触发的 UserScrollNotification 不再被误判为用户上滑，回看暂停只由真实指针拖动触发 |
| 弹幕 WS 走应用代理 | 弹幕 WebSocket 与 API/封面共用同一套应用代理设置（上游 3.1.0），配代理的场景下弹幕不再直连 |

---

## 📺 支持平台

- **哔哩哔哩（Bilibili）**
- **斗鱼直播（Douyu）**
- **虎牙直播（Huya）**
- **抖音（Douyin）**
- **快手直播（Kuaishou）**

支持按平台、分区筛选，各平台保持独立分页状态。

---

## 🎧 ASMR / 助眠模式

支持新房间自动进入纯音频、媒体保活、自定义自动停止时间、后台持续播放。房间内的耳机图标可单独切换当前房间的纯音频状态。

---

## 💾 数据管理

入口：设置 → 数据与维护 → 备份与恢复。

- 跨端扫码同步：一台设备「接收数据」出二维码，另一台「发送数据」扫码，完整覆盖关注/登录/全部设置
- WebDAV 云端备份与恢复
- 本地配置备份 / 恢复
- 缓存管理：设置 → 数据与维护 → 缓存与数据管理

本地设置与关注数据使用 AES + 系统密钥加密存储，更安全。

---

## 🔧 自编译安卓端（不提供安装包）

本仓库**不提供预编译 APK**，请克隆源码后按下述步骤自行构建。

### 环境要求

| 依赖 | 版本要求 | 说明 |
| --- | --- | --- |
| Flutter SDK | 3.x（实测 3.47.0） | [flutter.dev](https://flutter.dev) 安装，或使用国内镜像解压即用 |
| Android SDK | cmdline-tools + API 34+ + build-tools | 通过 Android Studio 或单独安装 cmdline-tools 后 `sdkmanager` 拉取 |
| JDK | 17+ | AGP 要求；Android Studio 自带的 JBR 即可 |
| Git | 任意近期版本 | 克隆仓库 |

安装 Flutter 后确保 `flutter` 在 PATH 中（Windows 下即 `<flutter>/bin`），并先跑一次 `flutter doctor` 确认 Android 工具链全部打勾。

### 构建步骤（Windows / Linux / macOS 通用）

```bash
# 1. 克隆仓库（国内网络建议走 gitee，GitHub 可用加速镜像）
git clone https://gitee.com/superxyline/purelive.git
cd purelive

# 2. 拉取依赖
flutter pub get

# 3. 静态检查（构建门禁：要求 lib/ 零 error，有 error 先修复再往下走）
flutter analyze --no-pub

# 4. 构建 arm64 release APK（无特殊情况只构建 arm64）
flutter build apk --release --target-platform android-arm64
```

构建产物位于：

```
build/app/outputs/flutter-apk/app-release.apk
```

> 只构建单一架构（如 `--target-platform android-arm64`）时产物名为 `app-release.apk`；同时构建多架构时会带 ABI 后缀（如 `app-arm64-v8a-release.apk`）。

安装到已连接的调试设备：`flutter install --release`，或直接 `adb install -r <上述apk路径>`。

### 构建说明与常见问题

- **为什么只出 arm64**：主流安卓手机 / 平板均为 64 位 ARM，只构建 arm64 可显著缩短编译时间与包体；如确需其他架构，调整 `--target-platform`（如 `android-arm`、`android-x64`），产物名会相应变化。
- **签名**：正式签名使用仓库内 `android/key.jks`（请妥善保管密钥，勿外传）。
- **Gradle 依赖下载慢 / 超时**：仓库 `tool/gradle-cn-mirrors.init.gradle` 提供国内镜像初始化脚本，可配合 Gradle 的 `--init-script` 参数使用；或为 `ANDROID_HOME` 配置国内代理镜像。
- **Windows PowerShell**：以上命令直接可用；若提示找不到 `flutter`，先执行 `$env:Path += ";<flutter路径>\bin"`。
- **改动了 `lib/` 之后**：提交或打包前重新跑第 3、4 步双验证（analyze 零 error + arm64 release 构建通过），避免引入启动崩溃类回归。

---

## 鸣谢

- 上游项目：[pure_live](https://github.com/liuchuancong/pure_live)（@liuchuancong）
- 参考项目：[dart_simple_live](https://github.com/xiaoyaocz/dart_simple_live)

---

## License

本项目基于上游开源项目二次开发，遵循上游的开源协议（见 [LICENSE](LICENSE)）。

> 仅供学习与交流使用，请遵守相关平台的服务条款与当地法律法规。
