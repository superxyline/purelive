# 纯粹直播（纯净版） Pure Live Pure

基于 [liuchuancong/pure_live](https://github.com/liuchuancong/pure_live) 二次开发、按个人使用需求裁剪的第三方多平台直播聚合播放器，使用 Flutter 构建。

> 本版本使用独立包名 `com.superxyline.purelive`，可与原作者版本**共存安装**，互不影响。

## 当前版本

- 版本号：**2.6.5**（构建号 23）
- 应用名：纯粹直播（纯净版）
- 签名：独立新签名密钥（仅保存在本机构建机，不入仓库）
- 支持平台：Android（含 Android TV）

## 支持平台

- B站（Bilibili）
- 斗鱼（Douyu）
- 虎牙（Huya）
- 抖音（Douyin）

## 功能列表

### 保留功能

- 多平台直播聚合播放，支持分区浏览、热门推荐、搜索
- 多平台直播搜索：B站/斗鱼/虎絪支持主播名、关键词模糊搜索及房间号精确搜索；抖音搜索需先在账户页登录抖音
- 多播放器内核切换：**MPV / IJK / EXO**
- 弹幕：显示、过滤、屏蔽词、显示优化
- 仅音频播放模式（后台听直播）
- 收藏 / 自定义标签
- 本地备份与恢复、WebDAV 同步、电视端数据扫码同步
- 工具箱：直播链接识别与解析
- 定时关闭、深色模式、自定义主题、播放器手势等

### 新增功能（相对原版）

- **B站醒目留言（SC）显示**：弹幕列表以 SC 卡片展示（B站主题渐变背景、头像、用户名、价格、留言正文）；全屏播放时在屏幕左下角弹出 SC 卡片，按 SC 有效时间自动消失
- **B站弹幕发送**：播放器底部控制条内置弹幕输入框（与弹幕开关/设置同排、随控制条自动隐藏），发送纯文本弹幕，颜色与官方一致
- **斗鱼网页登录**：账户页支持网页内嵌登录（WebView）与手动设置 Cookie，登录态本地持久化并纳入备份恢复
- **虎牙网页登录**：账户页支持网页登录页内嵌登录，登录后自动抓取多域名 Cookie 并保存
- **手机 / 平板横竖屏适配**：手机进入全屏自动横屏、退出全屏恢复竖屏；平板全程以横屏平板模式运行
- **本地数据安全增强**：Cookie 与 WebDAV 密码 AES 加密存储、备份导出脱敏、默认仅允许 HTTPS、关闭系统备份、日志安全加固
- **多平台关注同步**：登录 B站/斗鱼/虎牙后，可在账号页或 Cookie 设置页一键把平台关注的主播同步到应用收藏（按平台+房间号自动去重，只增不删；B站同步时自动过滤一周内未直播的主播）
- **已开播时长角标**：关注页直播封面左上角显示正在直播主播的开播时长（中文、精确到分钟、每分钟自动刷新），B站/斗鱼/虎牙/抖音均支持；可在设置 → 通用 中开关显示
- **跨端数据传输**：备份页"发送数据"扫描对方二维码，可将本机关注与登录数据完全覆盖到对方设备；"接收数据"生成二维码等待对方扫码（同一局域网）
- **双开观看**：播放页菜单"双开"可在主直播间上叠加一个静音小窗副直播间，两个直播间同时观看；小窗可自由拖动位置、默认停靠右下角不挡主画面，点击小窗互换主副（音频跟随焦点），可从收藏/搜索挑选副房间
- **双开副窗口可双指缩放**：双指揉合即可调整副窗口大小（宽度 130~600、16:9 同步）

### 已移除的功能（相对原版）

- 应用内更新（检查更新 / 下载安装）
- IPTV / M3U / EPG 直播源导入
- 网易 CC、快手直播平台
- 直播录制功能
- 历史记录功能（观看历史记录与页面）
- Firebase 登录与云配置同步

## 与原版的区别

| 项目 | 原版 | 本纯净版 |
| --- | --- | --- |
| 包名 | `com.mystyle.purelive` | `com.superxyline.purelive` |
| 应用名 | 纯粹直播 | 纯粹直播（纯净版） |
| 版本 | 跟随上游 | 独立版本线（当前 2.6.5） |
| 签名密钥 | 仓库内置 | 独立新密钥，仅本机保存 |
| 更新功能 | 有 | 无 |
| IPTV/M3U | 有 | 无 |
| 网易 CC / 快手 | 有 | 无 |
| 直播录制 | 有 | 无 |
| 历史记录 | 有 | 无 |
| Firebase 登录 | 有 | 无（各平台 Cookie 登录保留） |

## 最近更新（2.6.5）

- **修复从搜索进入直播间后返回键失效**：MIUI 等设备上，搜索页使用过输入法后，输入法的"视图显示"状态残留会持续拦截返回键（键盘事件到不了 App）。进入直播间前在键盘仍显示时主动断开输入法连接；播放器键盘层接管返回键，与左上角返回箭头走同一路由（全屏时仍为先退全屏再退直播间）
- **修复直播间音量键失效**：同样受输入法残留影响，物理音量键在直播间可能完全无反应。播放器键盘层接管音量键，按下后自行调整系统音量并同步播放器、弹出应用内音量条（系统/全局静音时保持静音）
- **Android 层返回键兜底**：统一返回入口直通 Flutter 路由（等效左上角箭头），兼容 MIUI 返回键"系统回调 + 按键事件"双路径，400ms 防重避免重复退出；framework 处理返回时注册高优先级返回回调，抢在输入法之前
- **修复音量相关**：进入直播间不再闪现音量条；房间记忆音量真正生效；修复音量条提示不显示的问题
- **物理音量键同步**：按物理音量键同步调整播放器音量并记忆该房间音量

## 最近更新（2.6.4）

- **修复直播间系统返回键失效**：此前在部分 Android 设备（如 MIUI 平板）上直播间内系统返回键、键盘快捷键全部失灵（键盘事件无法到达 Flutter，左上角箭头仍正常）。已移除播放器控制条的强制焦点抢占（`autofocus`），并在 Android 层为返回键增加与左上角箭头一致的兜底路由（兼容 MIUI 返回键"系统回调 + 按键事件"双路径，防重复退出）；普通状态返回退出直播间、全屏状态返回先退全屏再退直播间、首页返回退出 App
- **修复音量相关**：进入直播间不再闪现音量条；房间记忆音量真正生效（此前初始化阶段音量同步早于播放器启动且被防抖跳过，播放器可能以默认音量播放）；修复音量条提示不显示的问题
- **物理音量键同步**：按物理音量键会同步调整播放器音量并弹出音量条提示，同时记忆该房间音量（系统静音/全局静音时保持静音，不强行恢复记忆音量）
- **进入直播间自动收起残留键盘**：从搜索等带输入框页面进入直播间时自动收起软键盘，避免键盘残留影响返回体验
- **清理**：移除调试日志与临时文件，精简代码

## 最近更新（2.6.3）

- **双开副播放器可双指调整大小**：在副窗口上双指揉合（放大/缩小）即可调整小窗尺寸，以窗口中心为锚点缩放、按 16:9 同步联动，不会跑出屏幕；单指仍可拖动位置，整窗轻点仍是互换主副

## 最近更新（2.6.2）

- **修复多平台搜索失败**：B站改用 WBI 签名接口规避风控，解决“一开始能搜后面搜不了”的问题
- **斗鱼搜索优化**：改用稳定 Cookie（优先使用登录 Cookie，未登录时缓存稳定标识，不再每次随机），触发风控时给出明确提示
- **抖音搜索提示**：抖音搜索需要登录账号才可用，未登录时会明确提示“请先登录抖音”
- **搜索失败提示优化**：将原本掩码式的“请检查网络”改为具体的失败原因，例如风控、需要登录等

## 最近更新（2.1.5）

- **双开小窗放大 1.5 倍**：副窗口整体放大到原来的 1.5 倍（16:9 同步缩放），更易看清；默认停靠右下角、可自由拖动

## 最近更新（2.1.4）

- **双开小窗可拖动**：副窗口支持按住拖动到任意位置（限定在播放页可视范围内），默认停靠右下角，不遮挡主画面与清晰度栏；整窗轻点仍是互换主副，拖动与点击手势自动区分

## 最近更新（2.1.3）

- **新增双开观看**：播放页菜单"双开"选择一个副直播间，主+小窗同时观看（副窗口静音，音频始终来自主窗口）
  - 点副窗口互换主副：主窗口切到副房间出声，副窗口切回原主房间静音
  - 副窗口支持"换一个"替换副房间、"×"关闭退出双开
  - 选择面板提供 收藏 / 搜索 两种方式挑选副直播间（分区浏览待后续版本）

## 最近更新（2.1.2）

- **已开播时长角标**：关注页直播封面左上角显示正在直播主播的开播时长（中文、精确到分钟、每分钟自动刷新），B站/斗鱼/虎牙/抖音均支持
- **跨端数据传输**：备份页"发送数据"扫描对方二维码，可将本机关注与登录数据完全覆盖到对方设备（同一局域网）；"接收数据"生成二维码等待对方扫码
- **角标优化**：开播时长与热度值角标缩小为原来一半，时长角标移至封面左上角，窄屏卡片不再互相遮挡
- **显示开播时长开关**：设置 → 通用 新增"显示开播时长"开关，可随时控制直播封面"已开播时长"角标的显示（默认开启）
- **已关注标签**：关注页新增"已关注"标签，列出全部关注主播（纯列表展示，不含热度/开播时长角标，无需刷新即可查看管理）
- **修复**：虎牙开播时长不显示的问题；B站开播时长不显示的问题（改用 room_init 接口获取开播时间）

## 最近更新（2.1.1）

- **全新安装默认设置优化**：开机动画默认关闭，首次启动直达首页
- **弹幕默认显示优化**：打开直播间弹幕速度默认为 100、透明度默认 70%、显示区域默认 70%

## 最近更新（2.1.0）

- **新增多平台关注同步**：账号页与 B站/斗鱼/虎牙 Cookie 设置页新增"同步关注主播"入口，登录后可一键把平台关注的主播同步到应用收藏
  - B站：通过关注接口分页拉取并批量解析直播间，保留开播状态；同步时自动过滤一周内未直播的主播（正在直播的始终保留）
  - 斗鱼：通过斗鱼网页关注接口拉取（需登录 Cookie）
  - 虎牙：通过网页关注接口（TARS 协议）逐主播解析昵称/头像/房间号
  - 同步自动去重（平台+房间号）、只增不删，完成后汇总新增/已有/跳过数量，B站还会单独提示一周内未直播被过滤的数量

## 最近更新（2.0.0）

- **斗鱼网页登录**：账户页支持网页内嵌登录（WebView）与手动设置 Cookie，登录态本地持久化并纳入备份恢复
- **虎牙网页登录**：账户页支持网页登录页内嵌登录，登录后自动抓取多域名 Cookie 并保存
- **虎牙弹幕协议修复**：同步上游心跳数据与加房数据结构，虎牙弹幕连接更稳定
- **全屏交互优化**：点击画面可切换控制栏显隐；弹幕输入框激活后点击画面可取消焦点并自动隐藏控制栏
- **修复**：退出全屏后返回键偶发失灵（全屏状态同步复位）；进入直播间加载中返回偶发闪退（异步流程增加销毁防护）
- **界面调整**：设置页菜单顺序改为 通用设置 → 视频设置 → 主题设置 → 数据管理 → 备份管理 → 关于；首页收藏页移除"录播"标签；工具箱移除"支持解析列表"（快手/CC 相关文案清理）
- **修复**：备份恢复时登录 Cookie 不生效的历史问题（B站/虎牙/抖音/斗鱼 Cookie 均正常恢复）

## 最近更新（0.1.6）

- **修复全面屏手势返回**：普通状态交还系统原生返回（支持预测性返回），仅在全屏/半屏/画中画等需要拦截的状态由应用先处理；简化返回处理链路、删除冗余的应用级返回观察者，小米全面屏手势滑动返回恢复稳定
- **主界面左上角直达设置**：左上角按钮由弹出菜单改为直接进入设置页
- **关于入口移入设置页**：关于页从主界面菜单移除，改为设置页底部最后一个入口
- **移除历史记录功能**：删除历史记录页面、控制器、路由及记录逻辑，备份/恢复/旧版迁移/配置预览同步清理
- **设置页菜单重组**：刷新设置、导航栏显示设置、平台显示与授权、自定义网络代理并入"通用设置"；播放器内核并入"视频设置"；设置页顺序调整为 主题设置 → 视频设置 → 通用设置 → 数据管理 → 备份管理 → 关于
- **关于页更新日志补全**：补充 0.1.6 / 0.1.5.3 的更新记录

## 最近更新（0.1.5.3）

- **移除内置苹果字体**：改用系统默认字体，界面更统一、安装包更小
- **界面细节优化**：直播间右上角菜单文字垂直对齐；删除播放器底部投屏按钮（投屏统一为右上角菜单入口，并直接投当前直播地址）
- **关于页完善**：项目声明与开源许可证文案更新至最新情况，新增"更新日志"入口与页面

## 最近更新（0.1.5.2）

- **B站弹幕发送**：参照 PiliPlus 移植，播放器底部控制条内置弹幕输入框（普通/全屏同位置，与弹幕开关、设置按钮同排，随控制条自动隐藏），发送纯文本弹幕，颜色与官方 APP 一致
- **修复**：全屏播放时返回键退出失灵的问题；弹幕输入框不再整行常驻、改为随控制条自动隐藏
- **关于页更新**：项目声明与开源许可证文案更新至最新情况；新增"更新日志"入口与页面

## 最近更新（0.1.5.1）

- **B站全屏 SC 弹层**：全屏播放时收到醒目留言，在屏幕左下角弹出 SC 卡片，显示时长按 SC 有效时间（end_time）计算，到期自动消失；退出全屏或切换直播间时立即隐藏

## 最近更新（0.1.5）

- **B站醒目留言（SC）显示**：参照 PiliPlus 移植，进入 B站直播间自动加载最近醒目留言；实时 SC 以卡片形式显示在弹幕列表（B站主题渐变背景、头像、用户名、价格、留言正文），弹幕设置新增"显示醒目留言"开关（默认开启）

## 最近更新（0.1.4.2）

- **修复全屏方向**：手机进入全屏自动横屏、退出全屏恢复竖屏，且启动时即按设备类型锁定正确方向；平板（最短边 ≥600dp）全程以横屏平板模式运行

## 最近更新（0.1.4.1）

- **移除桌面端支持**：删除 Windows / macOS / Linux 平台代码与依赖，项目专注移动端（Android / Android TV）
## 最近更新（0.1.4）

- **修复**：退出直播间后仍残留后台音频的问题（非仅音频/后台播放模式下退出即停止播放）
- **GetX 改用 pub 包**：移除内置 GetX fork（lib/get），使用官方 get 依赖，便于跟随上游更新
- **抖音签名 JS 抽离**：douyin_sign.dart 由 642KB 拆分为独立 JS 文件，提升可维护性
- **JS 运行时复用**：抖音/斗鱼签名不再每次创建 4MB 运行时实例
- **依赖升级**：connectivity_plus 7 / permission_handler 13 / pro_mpack 3 / syncfusion 34 等
## 安全加固

- **签名密钥**：仓库与 APK 内不再包含任何密钥材料，使用独立新密钥签名
- **本地数据加密**：设置数据（含 Cookie、WebDAV 密码）使用 AES 加密存储，加密密钥保存在系统安全存储
- **备份脱敏**：备份/导出不再包含 Cookie 与 WebDAV 密码，恢复时也不覆盖当前账号信息
- **网络传输**：默认禁止明文 HTTP，仅允许 HTTPS
- **日志安全**：日志调试服务仅绑定本机回环地址，内存日志有上限
- **系统备份**：llowBackup 已关闭，防止备份被提取
- **精简**：移除无引用资源（CC/快手表情等）与 7 个冗余权限，保持最小权限

## 构建说明

环境要求：

- Flutter 3.44+（Dart 3.12+）
- JDK 17
- Android SDK（compileSdk 37 + build-tools）+ NDK

构建 release APK（按架构拆分）：

```bash
flutter pub get
flutter build apk --release --split-per-abi
```

产物位于 `build/app/outputs/flutter-apk/`：

- `app-arm64-v8a-release.apk`（主流手机 / 电视盒）
- `app-armeabi-v7a-release.apk`
- `app-x86_64-release.apk`

签名说明：

- 签名密钥保存在本机 `android/key.jks`，密码配置在 `android/key.properties`（两者均已 gitignore，不会入库）
- 新机器构建前需自行生成密钥：`keytool -genkeypair -v -keystore android/key.jks -alias key -keyalg RSA -keysize 2048 -validity 36500`

> 国内网络提示：项目已配置阿里云 Maven 镜像与 Gradle 国内镜像，GitHub 依赖可自行配置 ghfast 等加速通道。

## 声明与合规

- 本项目为**非营利性开源软件**，遵循仓库内 [LICENSE](LICENSE)（AGPL-3.0）。
- 内置/依赖的第三方开源组件声明见 **[THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)**。
- **不提供任何 VIP 解锁、视频破解或盗链服务**，高清直播需在对应平台拥有合法账号权限。
- 所有直播内容（视频、音频、图片等）**版权归属原平台所有**，本软件仅作技术聚合与转码展示。
- 用户 Cookie 仅用于本地请求身份认证（如 B站高清直播），**不会上传或存储到任何服务器**。
- 应用无广告、无追踪、无后台服务。若杀毒软件误报，请自行判断或拒绝使用。

## 实现说明

本纯净版（2.1.2）的功能裁剪、安全加固、代码优化与版本迭代，均由 **Codex（AI 编程助手）与 DeepSeek（AI 模型）** 协作完成，包括但不限于：

- 功能裁剪：更新、IPTV/M3U、网易 CC、快手、直播录制、Firebase 登录
- 安全加固：签名密钥轮换、本地数据加密、权限精简、明文 HTTP 收紧、日志安全
- 代码结构优化：GetX 去 vendored、抖音签名 JS 抽离、JS 运行时复用
- 依赖升级、体积优化与问题修复
## 致谢

- 原作者：[liuchuancong/pure_live](https://github.com/liuchuancong/pure_live)
- 参考项目：[dart_simple_live](https://github.com/xiaoyaocz/dart_simple_live)

本项目仅作个人学习与技术交流用途，请勿用于商业用途。
