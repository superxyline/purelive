{
  "name": "pure-live-nas-server",
  "version": "1.0.0",
  "description": "Pure Live NAS backend: aggregates five live platforms' APIs, proxies danmaku, and signs requests for the Web UI client.",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "node --test dist/test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/websocket": "^10.0.1",
    "fastify": "^4.28.1",
    "pino": "^9.4.0",
    "protobufjs": "^7.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
