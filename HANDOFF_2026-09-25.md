# 交接文档 — 2026-09-25（2.2.0 上游移植批 + 斗鱼弹幕发送调研收束）

> 仓库基线：`custom` 分支 HEAD = `4ff98698`，Gitee/GitHub 均已同步。
> 平板（21051182C）与手机（23127PN0CC）均已装 `2.2.0`；NAS 前后端均为最新。

## 一、2.2.0+4018 上游移植批（已完成，提交 451bbf30 / eb6ac743 / 9f6f485c）

从上游 liuchuancong/pure_live（3.1.8）移植六项，全部双端验证（analyze lib 零 error / build / tsc / vite）：

1. **抖音搜索三级回退**（双端）：安卓 `lib/core/site/douyin/douyin_search.dart`（新文件，上游 DouyinSearch 整体移植）+ server `douyin.ts` 同款 TS 实现。匿名 live 搜索 → 通用流式搜索 → 分区检索（双域名），风控静默降级不抛错。
2. **纯 Dart 签名**：抖音 a-bogus 换 `lib/core/utils/douyin/abogus.dart`（ABogus 类）+ `douyin_utils.dart` 的 `signFullUrl`（3 处调用点替换）；斗鱼换 `lib/core/site/douyu/douyu_utils.dart`（DouyuUtils：getEncryption 描述符 + getH5PlayV1 + parsePlayUrl 防双重拼接）。`core/scripts`（flutter_js）已零引用。
3. **重复/相似弹幕过滤**（双端）：安卓 `repeated_danmaku_filter.dart` + `danmaku_similarity_filter.dart`（上游拷贝）+ DanmakuSettings 6 个新 key + 设置页两组开关 + controller 接线；Web 端 Room.vue 屏蔽面板两开关（5s 重复 / 8s bigram 相似）。
4. **关注复合身份**：LiveRoom 新增 `identityKey/hasSameIdentity`；isFavorite/updateRoom 双条件；标签键迁移 `platform:roomId`（setRoomTags 带 legacyRoomId，读侧兼容旧键）。
5. **竖屏沉浸核心集**：`lib/player/core/portrait_stream_support.dart`（上游 830 行纯逻辑库整体入库）+ `lib/player/utils/orientation_policy.dart`（新写）：全屏方向三策略（portraitFullscreenPolicy，默认 landscape 保旧行为）+ 每房间方向覆盖（控制条「方向」按钮循环，存 roomOrientationOverrides）+ 竖屏全屏 ambient 封面模糊背景（portraitAmbientBackdrop）。**未接**：竖屏源自动识别三档布局、下滑隐藏弹幕栏手势（上游几何链差异大，专项评估）。
6. **四项修复**：弹幕 WS 走应用代理（web_socket_util + proxy_routing.dart）；回看误暂停（isDanmakuUserScrollStart 删 UserScroll 分支）；平板下拉刷新/启动竞态核查后确认**不适用/已对齐**。

版本 `2.2.0+4018`，README + RELEASE_NOTES 已更新。

## 二、斗鱼弹幕发送（调研收束，功能放弃）

**结论：不做。** 详见 `docs/DOUYU_SEND_RESEARCH_2026_09_25.md`（完整协议数据/错误码/样本）。

- 老通道 danmuproxy:8506 + chatmessage 被防御塔反作弊**静默丢弃**（双 IP 验证）。
- 网页实际走 **wss://wsproxy.douyu.com:6672/6673**，帧格式同 689；loginreq 全字段已破解（jwt/ltkid/devid/stk/username 均直接来自 cookie `acf_*`）；**chatmessage 新字段集抓到一条**（type 不在帧首、dy@/sender@ 替代 roomid/uid）。
- **卡点**：`vk` 算法（md5 类，两样本在 docs 里，16 字段拼接 × 9 盐枚举未中）+ chatmessage 稳定复采。需逆向页面 JS。
- 另两个污染源教训：810975 是短号（真实号 48699）且**未开播**；**NAS 托管的斗鱼 cookie 会过期**（JWT 约 7-30 天），过期报 4206。
- 现状：server `douyu_send.ts` 保留"诚实版"（老通道+回显校验+节流+转义，发送失败明确提示）；WebUI 发送框仅 B站（4ff98698）；安卓 DouyuChatSender 未接线（集成指南 docs/DOUYU_DANMAKU_SEND_INTEGRATION.md，实现需换新协议）。

## 三、其他本轮变更

- WebUI：播放器浮层（飘屏弹幕无用户名/左下礼物卡片价格分档/右上双开关）、弹幕回看+屏蔽体系、清晰度/音量记忆、7s 无帧换线 watchdog、搜主播、退出登录、关注服务端持久化（PUT/POST /api/sync/follows）+每分钟开播轮询、HEVC 开关、CDN 测速（/api/sites/speedtest）——均已上线。
- 安卓：网页搜索断链修复（外开浏览器）、TV 死入口删除、NAS 常连同步（NasSyncController：地址持久化+关注并集同步+变更防抖推送；修复 App 推送键名 bug——NAS 兼容 favoriteRooms/list）。
- NAS：webui-dist 结构改 `static/`（vite base './' + assetsDir）；部署后必须 `sudo docker compose up -d --force-recreate web`。

## 四、遗留/验证债

- 斗鱼发送如重启：按 docs/DOUYU_SEND_RESEARCH_2026_09_25.md §四 路径（页面 JS 逆向 vk → chatmessage 复采 → server 换新协议 → 安卓）。
- 竖屏沉浸未接的两块（自动识别布局/下滑手势）。
- NAS 托管的斗鱼 cookie 已过期（JWT exp 10-01 前有效的是用户 9-25 新登录的；NAS 里还是 9-04 过期的旧值）——如需斗鱼登录态功能先在账号页重新粘贴。
- 手机/平板已装 2.2.0，未做用户逐项验收。

## 五、环境备忘

- 工具：Flutter `E:\codex\tools\flutter\bin`、Node `E:\codex\tools\node\node-v20.18.1-win-x64`、adb `E:\codex\tools\android-sdk\platform-tools\adb.exe`、NAS SSH `python E:\codex\tools\nas_ssh.py`（docker 需临时 sudo 包装脚本，用完删）。
- adb 无线调试：平板 990c20c5（mDNS 自动连）、手机 23127PN0CC（192.168.31.189，已配对）、**192.168.31.237 是小米电视（MiTV，armeabi-v7a），arm64 包装不上**，勿混淆。
- CDP 抓网页 WS 帧（本轮验证有效）：`msedge --remote-debugging-port=9222 --user-data-dir=<非默认目录>`（**Chrome/Edge 136+ 禁止默认 profile 配调试端口**）→ Node `ws` 连 `/json` 的 page target → `Network.webSocketFrameSent/Received`（比页面 hook 可靠，弹幕 WS 可能在 Worker）→ `Network.setCookie` 注入登录态（session cookie，重启浏览器即失效）。坑：bash heredoc 会吞 `\\`（脚本里避免 `\\s` 正则）；node 脚本必须放在有 node_modules 的目录跑（nas/server）。
